import 'package:flutter/material.dart';

class TagStampModalInfo {
  Text? title;
  Widget? icon;
  Text? bodyMessage;
  Widget? btn;

  TagStampModalInfo({
    this.title,
    this.icon,
    this.bodyMessage,
    this.btn
  });
}
